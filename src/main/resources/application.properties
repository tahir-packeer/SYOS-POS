# File: src/main/resources/application.properties
# Database Configuration
db.url=jdbc:mysql://localhost:3306/syos
db.username=root
db.password=9900@tahir
db.driver=com.mysql.cj.jdbc.Driver

# HikariCP Configuration (for future concurrent access)
db.pool.maximumPoolSize=10
db.pool.minimumIdle=2
db.pool.connectionTimeout=30000
db.pool.idleTimeout=600000
db.pool.maxLifetime=1800000

# Business Rules
business.reorder.threshold=50
business.discount.rate=0.0
business.tax.rate=0.0

# Discount Rules (Manual Only)
# Automatic discount tiers are disabled
# business.discount.tier1.threshold=1000
# business.discount.tier1.percentage=2.0
# business.discount.tier2.threshold=2000
# business.discount.tier2.percentage=5.0
# business.discount.tier3.threshold=5000
# business.discount.tier3.percentage=10.0

# Application Settings
app.name=SYOS - Synex Outlet Store
app.version=1.0.0